{
  "name": "buffer-mcp-server",
  "version": "0.1.0",
  "description": "Thin-bridge MCP server for the Buffer GraphQL API. Reads the schema through introspection and exposes queries and mutations as MCP tools automatically.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "graphql": "^16.8.0",
    "graphql-tag": "^2.12.6"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
